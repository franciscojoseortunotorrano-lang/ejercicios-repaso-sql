DROP FUNCTION IF EXISTS divisible;

DELIMITER $$

CREATE FUNCTION divisible(num INT, num2 INT) RETURNS BOOLEAN
BEGIN 
	
	IF(num%num2=0)THEN
		RETURN TRUE;
	ELSE
		RETURN FALSE;
	END IF;
	
END $$

DELIMITER ;

